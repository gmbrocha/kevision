# Revision Sets

This directory is the future home for source drawing packages.

The active repo still uses the root-level `revision_sets/` path for
compatibility with existing CloudHammer defaults and regression tests.
