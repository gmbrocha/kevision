# Webapp

`webapp/` contains the current review UI surface.

Today it is a Flask app backed directly by `backend/` workspace objects. The
next stage is to keep the UI here while continuing to harden the backend
interfaces it depends on.

Related docs:

- `../docs/README.md`
- `../docs/architecture.md`
- `../backend/README.md`
